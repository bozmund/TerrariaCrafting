# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.3
loader_version=0.19.5
loom_version=1.17-SNAPSHOT

# Mod Properties
version=0.1.0
group=com.eboac

# Dependencies
fabric_api_version=0.161.0+26.3

# NOTE: Java 25 is selected via the toolchain in build.gradle, not here.
# A user-level ~/.gradle/gradle.properties on this machine pins org.gradle.java.home
# to JDK 21 for an unrelated Flutter project, and user-level properties override
# project-level ones -- so setting org.gradle.java.home here would have no effect.
